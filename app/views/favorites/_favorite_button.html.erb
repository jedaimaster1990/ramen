<% if current_user.favorite?(post_image) %>
  <%= link_to "お気に入りから外す", post_image_favorites_path(post_image), method: :delete %>
<% else %>
  <%= link_to "お気に入りにする", post_image_favorites_path(post_image), method: :post %>
<% end %>